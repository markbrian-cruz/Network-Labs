# NTP_Associations Validation Report
Generated: 2026-03-01 15:51:18.882609

## SP1-PE1 (PE)
```

      address         ref clock     st  when  poll reach  delay  offset    disp
*~172.16.100.2     112.210.51.208    3    38   128  377    40.0   15.26    18.0
 * master (synced), # master (unsynced), + selected, - candidate, ~ configured
```

## SP1-BR2 (BR)
```

      address         ref clock     st  when  poll reach  delay  offset    disp
*~172.16.100.2     112.210.51.208    3     6   128  377    39.8   -6.08    17.9
 * master (synced), # master (unsynced), + selected, - candidate, ~ configured
```

## SP1-BR3 (BR)
```

      address         ref clock     st  when  poll reach  delay  offset    disp
*~172.16.100.2     112.210.51.208    3    17    64  377    35.7   -4.15    20.5
 * master (synced), # master (unsynced), + selected, - candidate, ~ configured
```

## SP1-RR4 (RR)
```

      address         ref clock     st  when  poll reach  delay  offset    disp
*~172.16.100.2     112.210.51.208    3     9    64  377    11.7    8.90     9.8
 * master (synced), # master (unsynced), + selected, - candidate, ~ configured
```

## SP1-PE5 (PE)
```

      address         ref clock     st  when  poll reach  delay  offset    disp
*~172.16.100.2     112.210.51.208    3    29    64  377    46.9   21.27     2.3
 * master (synced), # master (unsynced), + selected, - candidate, ~ configured
```

